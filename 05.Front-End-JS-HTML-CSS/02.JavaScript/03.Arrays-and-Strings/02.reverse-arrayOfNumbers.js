function reversing(n, arrNums) {
    let reversedArray = [];
    for (let i = 0; i < n; i++) {
        reversedArray += i;
    console.log(reversedArray);
        
    }
}

reversing(3, [10, 20, 30, 40, 50]);